version: 1
steps:
  - id: ensure-user-group
    type: ensure_user_group
    user: globular
    group: globular
    home: "{{.StateDir}}"
    shell: /usr/sbin/nologin
    system: true

  - id: ensure-dirs
    type: ensure_dirs
    dirs:
      - path: "{{.Prefix}}"
        owner: root
        group: root
        mode: 0755
      - path: "{{.Prefix}}/bin"
        owner: root
        group: root
        mode: 0755

      - path: "{{.StateDir}}"
        owner: globular
        group: globular
        mode: 0750
      - path: "{{.StateDir}}/cluster-controller"
        owner: globular
        group: globular
        mode: 0750

      - path: "{{.ConfigDir}}"
        owner: root
        group: root
        mode: 0755
      - path: "{{.ConfigDir}}/cluster-controller"
        owner: root
        group: root
        mode: 0755

  - id: stage-cluster-controller-package
    type: stage_package
    path: "internal/assets/packages/service.cluster-controller_{{.Version}}_linux_amd64.tgz"
    require_platform_match: true
    require_type_service: true

  - id: install-cluster-controller-payload
    type: install_package_payload
    install_bins: true
    install_config: true     # <-- IMPORTANT: install config/cluster-controller/config.json from package
    install_spec: false
    install_systemd: false

  - id: install-cluster-controller-service
    type: install_services
    units:
      - name: globular-cluster-controller.service
        owner: root
        group: root
        mode: 0644
        atomic: true
        content: |
          [Unit]
          Description=Globular Cluster Controller (embedded)
          After=network-online.target
          Wants=network-online.target

          [Service]
          Type=simple
          User=root
          Group=root
          WorkingDirectory={{.StateDir}}/cluster-controller

          ExecStart={{.Prefix}}/bin/cluster_controller \
            --config {{.ConfigDir}}/cluster-controller/config.json \
            --state {{.StateDir}}/cluster-controller/state.json

          Restart=on-failure
          RestartSec=2
          LimitNOFILE=524288

          [Install]
          WantedBy=multi-user.target

  - id: start-cluster-controller
    type: start_services
    services:
      - globular-cluster-controller.service
    restart_on_files:
      globular-cluster-controller.service:
        - "{{.ConfigDir}}/cluster-controller/config.json"
    binaries:
      globular-cluster-controller.service: cluster_controller

  - id: health-cluster-controller
    type: health_checks
    services:
      - globular-cluster-controller.service

